const mockUser = require('./mockUser');
const mockItem = require('./mockItem');
const mockCategory = require('./mockCategory');
const items = require('./itemData')

module.exports = { mockUser, mockItem , mockCategory, items };
